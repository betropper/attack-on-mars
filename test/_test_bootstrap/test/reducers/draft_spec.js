import chai from 'chai';
const assert = chai.assert;

import draft from '../../src/js/reducers/index';

describe('reducers/draft', () => {
     it('tests', () => {
        assert.isTrue(false, 'needs tests');
    });
});